#import all the stuff

class Board(object):
    """
    Represents the Catan board. Keeps track of tiles, ports, and pieces. 

    (Add more stuff)
    """

    #Maybe we want to have more arguments for debugging purposes, but we don't need if for gameplay
    def __init__(self)